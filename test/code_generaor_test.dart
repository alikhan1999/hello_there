import 'package:flutter_test/flutter_test.dart';
import 'package:empathyGenerator/all_utils.dart';

void main() {
  test('Random color code test', () async {

    // final color=ColorGenerator.generateRandomColor();
    // expect(color, isA<ColorModel>());
    // Verify that our Color code comes.

  });
}